# MongoDB Configuration
# For local development, use: mongodb://localhost:27017/kejah
# For MongoDB Atlas, use: mongodb+srv://username:password@cluster.mongodb.net/kejah
MONGODB_URI=mongodb://localhost:27017/kejah

# JWT Secret for authentication (change this in production!)
JWT_SECRET=your_secret_key_here

# Server Configuration
PORT=5000
NODE_ENV=development

# Frontend URL for CORS
FRONTEND_URL=http://localhost:3000

# Payment Configuration
# Options: demo, mpesa
PAYMENT_MODE=demo

# M-Pesa Configuration (only needed if PAYMENT_MODE=mpesa)
# MPESA_CONSUMER_KEY=your_consumer_key
# MPESA_CONSUMER_SECRET=your_consumer_secret
# MPESA_SHORTCODE=your_shortcode
# MPESA_PASSKEY=your_passkey
# MPESA_CALLBACK_URL=your_callback_url
